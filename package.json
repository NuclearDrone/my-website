{
  "name": "my-website",
  "version": "0.0.1",
  "description": "My website for learning fullstack web development",
  "main": "index.js",
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm start",
    "dev": "concurrently --kill-others-on-fail \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Micke Enberg",
  "license": "MIT",
  "dependencies": {
    "npm": "^8.11.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
